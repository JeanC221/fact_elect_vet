node_modules
.next
.env
.env.local
.env.*.local

.vercel
.env*

# TypeScript incremental build artifact
tsconfig.tsbuildinfo
*.tsbuildinfo

.DS_Store

# Local Siigo API capture dumps (scripts/capture-siigo-response.ts) — contain
# real production catalogue data (products, payment types, document types)
# and must never be committed.
/capturas/