node_modules
.next
.env
.env.local
.env.*.local

.vercel
.env*
# Excepción: la plantilla SIN valores debe versionarse (regla D6, chat 6a).
# Sin esta línea, `.env*` la ignora en silencio y nadie nota que falta.
!.env.example

# TypeScript incremental build artifact
tsconfig.tsbuildinfo
*.tsbuildinfo

.DS_Store

# Local Siigo API capture dumps (scripts/capture-siigo-response.ts) — contain
# real production catalogue data (products, payment types, document types)
# and must never be committed.
/capturas/